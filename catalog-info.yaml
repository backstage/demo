apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-demo
  description: An example component showcasing GitHub Actions workflows.
  annotations:
    # **GitHub repository identifier**; used by the GitHub Actions plugin to locate your repo.
    github.com/project-slug: bhavik-moodys/bs-demo
    # **Managed by location** provides a direct URL reference to your repository.
    backstage.io/managed-by-location: url:https://github.com/bhavik-moodys/bs-demo?ref=master
spec:
  type: service
  owner:  bhavik-moodys
  lifecycle: experimental
