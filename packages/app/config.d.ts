export interface Config {
  /**
   * @visibility frontend
   */
  notificationsTester?: {
    /**
     * Flag to enable or disable the tester
     * Default is enabled
     *
     * @visibility frontend
     */
    enabled: boolean;
  };
}
